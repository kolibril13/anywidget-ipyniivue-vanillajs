{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/*.js --minify --format=esm --bundle --outdir=src/anywidget_ipyniivue_vanillajs/static"
	},
	"dependencies": {
		"@niivue/niivue": "^0.38.8"
	},
	"devDependencies": {
		"esbuild": "^0.19.5"
	}
}
